import React from 'react';

// This type is exported to prevent breaking changes in other files during refactoring.
export type GenerationType = any;

// AI functionality has been removed by user request.
const ContentGeneratorModal: React.FC = () => {
  return null;
};

export default ContentGeneratorModal;